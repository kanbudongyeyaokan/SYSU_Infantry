该层为应用层，主要包括机器人各个模块的任务编写，比如底盘、云台、发射机构、遥控器数据解析等。  
> 目的：只需调用 **Funciton_Module** 层的接口即可完成任务,因此代码会非常简洁，所有功能都在Funciton_Module层实现，这里只做执行。

## ✅ 已完成：Motor Task 和 Chassis 模块拆分

你需要把当前的 motor task 系统进行拆分，包括（Motor Task 和 Motor Control 模块）
- ✅ 拆分成两个模块：
    - ✅ (Motor Task  && Motor Control)：负责具体的 can 发送，控制电机
    - ✅ (Chassis Task && Chassis)：负责底盘解算，接收决策层的控制量，解算出四个轮子的速度，然后通过静态全局变量传给 Motor Task

✅ 注意，你涉及到有关发布订阅模型的 API 接口，不要自己随便找个api写，必须使用我们系统中已有的发布订阅模型的 api，就在原本的未拆分的 motor task 代码中，已经有使用发布订阅模型的 api 了，你可以参考那里。

### ✅ motor task && motor control 执行的逻辑
- ✅ motor task 相当于 can 发送任务，一直发送所有 can 电机的控制量
- ✅ 频率 1000 Hz
- ✅ 一直运行，负责在这里直接控制电机的目标值
    - ✅ 类似 can motor control test 任务一样  
    - ✅ 各个电机的目标值来自于底盘解算后的结果，就是底盘任务先接收决策层发过来的vx,vy，然后底盘解算得到四个速度，用静态全局变量做
    - ✅ motor task 读取这个静态全局变量，然后发送给电机
- ✅ 并且这模块还负责具体修改电机的控制模式
    - ✅ 由于每个电机都可以单独设置控制模式（以及pid参数），但是具体是什么模式是决策层发给底盘任务的
    - ✅ 可以参考使用类似的静态全局变量，底盘任务设置这个变量，motor task 检查是否更新，如果更新了就读取这个变量，然后设置各个底盘电机的控制模式和pid参数，等待下一次更新

### ✅ Chassis Task && Chassis 
 - ✅ 目前的底盘是全向轮底盘，请你根据全向轮底盘的运动学模型，编写底盘解算代码
    - ✅ 我希望系统中有一个底盘解算的模块是可以有个框架进行配置的，后续可以配置成差速轮底盘，麦克纳姆轮底盘，全向轮底盘
    - ✅ 目前只需要实现全向轮底盘的解算
 - ✅ 负责底盘解算，接收决策层的控制量，解算出四个轮子的速度，然后通过静态全局变量传给 Motor Task

## 实现详情

查看 `拆分总结.md` 文件了解完整的实现架构和使用说明。

### 新增文件
- `motor_chassis_interface.c/h` - Motor Task 和 Chassis Task 之间的接口
- 重构 `chassis.c/h` - 支持多种底盘类型的运动学解算
- 重构 `Chassis_task.c/h` - 底盘控制任务实现
- 重构 `motor_task.c` 和 `motor_control.c/h` - 专注于电机控制

### 运行频率
- Motor Task: 1000Hz (CAN发送)
- Chassis Task: 500Hz (底盘解算)